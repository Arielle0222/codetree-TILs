#include <iostream>
using namespacce std;

int main() {
    int y,m,d;
    char s = "."
    cin >> y;
    cin.get();
    cin >> m;
    cin.get();
    cin >> d;

    cout << y << s << m << s << d;

    // 여기에 코드를 작성해주세요.
    return 0;
}